# Control Flow

## What is Control Flow?

Control flow determines **which code runs and when**.

It allows programs to make decisions and execute different code paths based on conditions.

---

## if Statement

```js copy
if (condition) {
  // runs if condition is truthy
}
```

```js copy
let age = 20;

if (age >= 18) {
  console.log("Adult");
}
```

### Truthy and Falsy in Conditions

Conditions are not limited to booleans.

```js copy
if ("hello") {
  // runs (truthy)
}

if (0) {
  // does not run (falsy)
}
```

## if...else

```js copy
if (condition) {
  // runs if true
} else {
  // runs if false
}
```

```js copy
let age = 16;

if (age >= 18) {
  console.log("Adult");
} else {
  console.log("Minor");
}
```

## else if

```js copy
if (score >= 90) {
  console.log("A");
} else if (score >= 75) {
  console.log("B");
} else {
  console.log("C");
}
```
